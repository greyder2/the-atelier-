import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Corporate Quotation',
  description: 'Request a customized language training quotation for your organization. Tailored programs for corporate teams and international companies.',
}

export default function CorporateQuotationLayout({ children }: { children: React.ReactNode }) {
  return children
}
